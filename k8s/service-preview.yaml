apiVersion: v1
kind: Service

metadata:
  name: demo-api-preview

spec:
  type: NodePort

  selector:
    app: demo-api
    slot: green

  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30091